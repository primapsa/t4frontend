import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  badge: {
    justifyContent: 'flex-end',
  },
  button: {
    marginLeft: '15px',
  },
  icon: {
    height: '15px',
  },
  input: {
    height: '22px',
    margin: '0 5px',
    minHeight: '15px',
  },
  wrapper: {
    alignItems: 'center',
    marginTop: '10px',
    width: '100%',
  },
}))
