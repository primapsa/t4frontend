import {createStyles} from "@mantine/core";

export const useStyles = createStyles(() => ({
    container:
        {
            alignItems: 'center',
            justifyContent: 'center',
            margin: '10px',
            height: '80vh'
        },
    wrapper: {
        width: '100%',
        maxWidth: '320px'
    }

}));