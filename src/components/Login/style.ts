import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  container: {
    alignItems: 'center',
    height: '80vh',
    justifyContent: 'center',
    margin: '10px',
  },
  wrapper: {
    maxWidth: '320px',
    width: '100%',
  },
}))
