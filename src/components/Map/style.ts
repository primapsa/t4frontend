import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  map: {
    alignItems: 'center',
    display: 'flex',
    height: '300px',
    justifyContent: 'center',
    margin: '30px 0',
    width: '100%',
  },
  mapContainer: {
    height: '100% ',
    width: '100% ',
  },
}))
