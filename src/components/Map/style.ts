import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  mapContainer: {
    height: '100%',
    with: '100%',
  },
  wrapper: {
    alignItems: 'center',
    display: 'flex',
    height: '300px',
    justifyContent: 'center',
    margin: '30px 0',
    width: '100%',
  },
}))
